#include <iostream>
using namespace std;
typedef int ElementType;
typedef struct {
    ElementType data;
    struct LNode *  next;
}LNode,*LinkList;
void Devid_List(LinkList *s,LinkList *r,LinkList *l){
    // 创造结点让后面的元素操作
  // LinkList pa = (*s).next;
  // LinkList la = l;
}